let hello = "Hello";
let world = "World";
let epicPhrase = hello + " " + world + " !";
console.log(epicPhrase);

//Template literals
let epicPhrase2 = `${hello} ${world} !`;
console.log(epicPhrase2);

//Multi-line strings Use french quotes
let lorem = "This is a  String \n " + " this is a line";
let lorem2 = `This is a epic phrase 
the continue from epic phrase.`;
console.log(lorem);
console.log(lorem2);